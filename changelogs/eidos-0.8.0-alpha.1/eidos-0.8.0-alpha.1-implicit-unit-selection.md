# Implicit Unit bodies and branch selection

- Added a verified public example for implicit `Unit` function bodies and unbraced `then` / `else` expression arms.
- Documented canonical Option, Result, and right-biased Either selection in both locales.
- Added `_N` selection placeholder highlighting to the website code renderer.
- Documented the VS Code Marketplace-required numeric `0.8.0` prerelease package version separately from the Eidos `0.8.0-alpha.1` language and other editor plugin versions.
- Sync-ID: `eidos-2026-07-23-implicit-unit-selection`.
