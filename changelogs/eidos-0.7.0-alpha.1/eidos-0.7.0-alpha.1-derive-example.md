# Eidos 0.7.0-alpha.1 derive example synchronization

- Updated the public derive example to the compiler-managed `meta.Type -> meta.Items` generator protocol and `@[expand(...)]` typed declaration tag.
- Use the final `meta.instance(trait, target, methods)` constructor without retaining the interim `meta.implementation` spelling.
- Refresh the public capability and diagnostic examples to identify the Eidos 0.7 compiler baseline.
- Regenerate the checked-in social preview assets after updating their Eidos 0.7 version label.
